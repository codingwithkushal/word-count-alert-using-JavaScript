function myFunction() {
    let words = prompt("Compose your message");
    let count = words.length;
alert(`You have written ${count} words. You have ${150 - count} words remaining.`);
  }

  
